# Java Team Profile

## Scope

- Backend services in Java, primarily Spring Boot.

## Default Toolchain

- `mvn test` or `gradle test`
- `spotbugs` / `checkstyle`
- `owasp dependency-check` or equivalent

## Quality Gate Additions

1. Build + unit tests mandatory
2. API contract compatibility checks for changed endpoints
3. Dependency/security scan before release

